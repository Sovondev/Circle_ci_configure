version: 2.1
jobs:
  build:
    docker:
      - image: ubuntu:latest
    steps:
      - checkout
      - run:
          name: "install gcc"
          command: apt-get install -y gcc-4.9
      - run:
          name: "Run C program"
          command: gcc -o output helloworld.c